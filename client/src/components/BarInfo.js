import React, { PropTypes } from 'react';

const BarInfo = ({ current }) => {
  return (
    <div>
      
    </div>
  );
};

BarInfo.propTypes = {
  current: PropTypes.any,
};

export default BarInfo;
